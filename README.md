# Claude Code 초보자 설정 가이드

`index.html` + `copy-prompt.js`(회색 상자 클릭 = 복사). OS 라디오로 맥/윈도 안내 분리.

**전:** `#cli`까지. 먼저 GitHub·Vercel 가입 + Claude Pro 로그인. 그다음 터미널 설치(맥 M4 기준: Xcode CLT·curl로 Node LTS·gh universal pkg·`npm i -g vercel`, 윈: winget). `gh auth login` 후 GitHub API로 `git config` 이름·이메일 자동(수동 입력 불필요).

**당일:** Code 탭 → GitHub 프롬프트 → Vercel → (선택) Supabase. (선택) [gstack](https://github.com/garrytan/gstack) README 참고.
